[package]
name = "vivid-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to vivid-core C API"
authors = ["Vivid"]
license = "MIT"

[dependencies]

[build-dependencies]

[features]
default = []
